#include "main.h"
/**
 *mul- multiplies two integers
 *@x: The first number to mutiply
 *@y: The second number to multiply with the first number
 *Return: The result of multiplication between x and y param
 */
int mul(int x, int y)
{
	return (x * y);
}
